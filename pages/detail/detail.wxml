<!--pages/detail/detail.wxml-->
<view class='detail'>
  <block wx:for="{{ nameId }}" wx:key='{{ index }}'>
    <view class='item'>
      <block wx:for="{{ item.opencode }}" wx:key="{{ index }}">
        <view>
          {{ item }}
        </view>
      </block>
    </view>
  </block>
</view>
